<!-- app/views/products/show.html.erb (trecho) -->
<%= form_with model: @line_item, url: line_items_path, data: { turbo_stream: true } do |f| %>
  <%= f.collection_select :product_variant_id,
        @variants, :id, :sku,
        prompt: "Escolha o grau" %>
  <%= f.number_field :quantity, value: 1, min: 1, class: "w-16" %>
  <%= f.submit "Adicionar", class: "btn-primary" %>
<% end %>