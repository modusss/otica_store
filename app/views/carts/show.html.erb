<h1 class="text-2xl font-semibold mb-4">Seu carrinho</h1>

<div id="cart_items">
  <%= render "carts/items", cart: @cart %>
</div>

<% if @cart.line_items.any? %>
  <%= link_to "Ir para o checkout",
        new_checkout_address_path,
        class: "mt-6 inline-block bg-primary text-white px-4 py-2 rounded" %>
<% else %>
  <p class="text-gray-500">Seu carrinho está vazio.</p>
<% end %>