<!-- Premium, minimal, responsive navbar -->
<nav class="bg-white shadow-sm border-b">
  <div class="container mx-auto flex items-center justify-between py-4 px-4">
    <%= link_to "Ótica", root_path, class: "font-extrabold text-2xl tracking-tight text-gray-900 hover:text-blue-700 transition-colors" %>
    <div class="flex items-center gap-6">
      <%= link_to "Produtos", products_path, class: "text-gray-700 hover:text-blue-700 font-medium transition-colors" %>
      <% if user_signed_in? %>
        <%= link_to "Meus pedidos", orders_path, class: "text-gray-700 hover:text-blue-700 font-medium transition-colors" %>
        <% if current_user.admin? %>
          <%= link_to "Painel Admin", panel_root_path, class: "text-gray-700 hover:text-blue-700 font-medium transition-colors" %>
        <% end %>
        <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "text-gray-700 hover:text-red-600 font-medium transition-colors" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "text-gray-700 hover:text-blue-700 font-medium transition-colors" %>
      <% end %>
      <%= link_to "Carrinho", cart_path, id: "mini_cart", class: "ml-2 px-3 py-1 rounded-full bg-blue-50 text-blue-700 font-semibold hover:bg-blue-100 transition" %>
    </div>
  </div>
</nav>